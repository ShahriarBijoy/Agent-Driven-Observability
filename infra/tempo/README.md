# infra/tempo

Tempo (traces) config + metrics-generator — Phase 2.

This directory will contain the Tempo configuration for distributed trace storage,
including the metrics-generator for deriving RED metrics from trace data.
