# infra/marquez

Marquez (OpenLineage backend) config + init SQL — Phase 3.

This directory will contain the Marquez server configuration and database
initialization SQL for storing and querying OpenLineage dataset/job metadata.
