# infra/alloy

Grafana Alloy (OTel collector) config — Phase 2 (config.alloy).

This directory will contain the Alloy pipeline configuration for collecting and routing
OpenTelemetry signals (traces, metrics, logs, profiles) to the appropriate backends.
