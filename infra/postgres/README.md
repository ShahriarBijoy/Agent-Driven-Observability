# infra/postgres

Postgres init scripts (pgvector, agent tables) — Phases 1, 5.

This directory will contain SQL init scripts: Phase 1 installs the pgvector extension
and creates the core agent/session tables; Phase 5 adds evaluation and experiment tables.
