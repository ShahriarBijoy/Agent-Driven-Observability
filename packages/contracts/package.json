{
  "name": "@obs/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"@obs/contracts: build (noop)\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@obs/domain": "workspace:*"
  },
  "devDependencies": {
    "@obs/tsconfig": "workspace:*"
  }
}
