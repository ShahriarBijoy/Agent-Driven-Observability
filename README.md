# Agent-Driven-Observability
 The subject system is an AI inference gateway — a tiny LLM proxy with a RAG pipeline, embedding cache, rate limits, and synthetic faults. The observation system is the full Grafana LGTM stack plus OpenLineage / Marquez for data lineage. The intelligence layer is a Python service hosting five agents built on the Claude Agent SDK.
