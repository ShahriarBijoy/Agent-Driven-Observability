{
  "name": "@obs/model-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"@obs/model-proxy: dev placeholder (implemented in a later phase)\"",
    "build": "echo \"@obs/model-proxy: build (noop)\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@obs/contracts": "workspace:*",
    "@obs/telemetry": "workspace:*"
  },
  "devDependencies": {
    "@obs/tsconfig": "workspace:*"
  }
}
