[project]
name = "agent-service"
version = "0.0.0"
description = "Claude Agent SDK host for the AI Observability Lab (Phase 5)."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
